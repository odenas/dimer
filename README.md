# Deep models for gene regulation (dimer)
`dimer` is a library for modeling genomic data with deep architectures. 

This repository is a port of https://bitbucket.org/gertidenas/dimer/ using `pytorch`.
